name: keycase_server
description: KeyCase identity server — stores and serves cryptographic identity proofs. Self-hostable.
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_static: ^1.1.2
  args: ^2.4.2
  dotenv: ^4.2.0
  postgres: ^3.1.0
  keycase_core:
    git:
      url: https://github.com/keycase/core.git

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
  http: ^1.2.0
